import 'package:flutter/material.dart';

class YemekAyrinti extends StatelessWidget {
  const YemekAyrinti({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
